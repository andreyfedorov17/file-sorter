# File Sorter Application
A console application that sorts files by their extensions in the specified directory and puts them in directories with such extensions.
## Technologies
* Java 8
## How to use the app
1. Open the shell in the src directory
2. Run the program: javac Main.java -> java Main
3. Enter the path of the directory you want to sort
